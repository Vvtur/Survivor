# 2026-08-28

## 项目整体勘察（Survivor = 2D 生存类微信小游戏）
- 引擎 Unity 6000.3.16f1 + URP 2D；框架 QFramework 自定义版（ResKit/UIKit/AudioKit/ActionKit/FSMKit）。
- 目标平台微信小游戏（WX-WASM-SDK-V2 0.1.1），Boot 场景唯一进主包，GameStart/MainGame 走 AB 异步加载。
- 自有代码约 3400 行（不含 InputSystem 生成的 1838 行）。分层：Scripts/Game（架构层：Model/System/Command/Utility）+ Scripts/Gameplay（表现层）+ Res/Scripts/UI（UIKit 面板）。

## 当前工作区状态
- main 分支，12 次提交，最新 a09bb8b「备案」。有 14 个文件未提交改动，是一次 QFramework 架构合规性修复（详见 docs/QF-架构修复报告.md）。
- 修复要点：WaveSystem 越层 SendCommand → 改为 SendEvent + WaveDriver 转发 Command；PlayerInfoPanel 改为 Awake 单入口 + Unity OnDestroy 注销；删空壳 GameManagerSystem；BuyCommand 字段改 mCost。
- 这些改动未提交，也没有在 Unity 里编译验证过（本机 Editor.log 属于另一个 2021.3 工程，无法佐证）。

## 发现的问题（按严重度）
1. 🔴 PlayerController.GameOver()：`StartCoroutine(UIKit.OpenPanelAsync<GameOverPanel>())` 紧接着 `gameObject.SetActive(false)`。SetActive(false) 会停掉该 MonoBehaviour 上所有协程；首次死亡时面板 AB 未缓存 → OpenPanelAsync 会 yield WaitForEndOfFrame → 协程被杀 → 结算面板打不开且 timeScale=0 卡死。第二次以后因面板已存在、回调同步完成而正常。代码注释自称"协程宿主用常驻 GameRoot"，但实际没改。
2. 🔴 表现层绕过 Command 直接写 Model：`PlayerController.OnTriggerStay2D` 里 `mModel.HP.Value--`（应发 DamagePlayerCommand）。
3. 🟡 文档过期：docs/CODE_WIKI.md 仍写 GameManagerSystem 注册、"3 个全局事件"（实际 4 个），未同步修复报告。
4. 🟡 GameRoot 里 OnSceneLoaded 整段被注释，而 PlayerController.OnDestroy 的注释仍指向它；空的 Awake/OnEnable/OnDestroy 残留；mSceneLoader 从不 Recycle2Cache，场景 AB 常驻内存。
5. 🟢 WaveSystem 到胜利时间后每帧 SendEvent(GameWinEvent)（GameOver 有 isOver 守卫，只是浪费）。
6. 🟢 ProjectSettings 里 projectName 仍是 "My project"（刚写入 organizationId: vvtur）。

## 21:40 接入 Unity MCP（CoplayDev/unity-mcp）后复核
- 已联通并实测通过。安装包 com.coplaydev.unity-mcp@10.1.2（README 写最新版 v10.0.0，实际装的是 10.1.2）。
- Unity 端走本地 HTTP/WS，**实际端口 7070**（不是 README 默认的 8080）。
- 编辑器现状：Unity 6000.3.16f1、工程 E:/UN/Survivor、活动场景 Boot、未播放、BuildTarget=WebGL、Scripting Define Symbols 为空（未启用 USE_ROSLYN）。
- **编译结论（推翻上面第 11 行的"未验证"）**：那批未提交改动**已成功编译**。用 execute_code 反射验证：`SpawnEnemyRequestEvent` / `WaveDriver` / `IWaveSystem` 均存在于 Assembly-CSharp，`GameManagerSystem` 已不存在；Assembly-CSharp.dll 编译于 21:07:40。控制台 4 条日志全是 MCP 启动信息，无 error / warning。
- Roslyn 未装 → execute_code 回退 **CodeDom = C# 6 语法**。踩坑：不能用 C#7+ 语法；Unity 6 删掉了 `PlayerSettings.GetScriptingDefineSymbols(BuildTargetGroup)` 重载，必须写 `UnityEditor.Build.NamedBuildTarget.FromBuildTargetGroup(...)`。
- 更正 wiki 错误：场景实际在 `Assets/Scenes/`（Boot / GameStart / MainGame / SampleScene），CODE_WIKI.md 里的 `Assets/Res/Scenes/` 不存在。
- MainGame.unity 中确有 WaveDriver 与 PlayerInfoPanel 引用（印证修复报告"面板挂场景、不走 UIKit 生命周期"的说法）。

## 待确认
- 用户是否希望我修上面的 1/2 项，以及是否要先把未提交的架构修复 commit 掉。

## 23:00-23:40 UI 打磨落地（用户确认方案后执行完毕）
**弹窗动画**：新建 `Assets/Res/Scripts/UI/UIPanelAnim.cs`（PlayOpen=scale OutBack+fade、PlayClose=收缩后回调再关、PlayIntro=错峰入场，全部 SetUpdate(true) 免疫 timeScale=0，CanvasGroup 运行时自动补挂）。5 个弹窗面板 OnShow 接 PlayOpen；关闭按钮改"先播动画→onComplete 再关"（ShopPanel 保持 Hide() 语义；GameLevelUpPanel 的 timeScale=1 挪到动画后；GameOverPanel 只加打开动画，重启流程没动——先关面板再切场景会白屏等待）。
**开始界面**：GameStartPanel 重排——新增标题"幸存者"（FZSTK SDF，可改名）+ 开始按钮居中(340×96, y=-80) + 排行榜/商店/邀请底部横排(-300/0/+300, y=90)；新增全屏透明 Img_ClickBlocker（第一个子节点）接住切场景残留点击（替代原 Btn_Start 铺满全屏 hack，顺带保护无守卫的 Shop 按钮）；Start() 错峰入场动画，防误触窗口 0.5→0.75s。
**主游戏 HUD**：PlayerInfoPanel 改为全屏——顶部全宽经验条（Img_ExpBG 深色底 + Img_ExpFill Filled Horizontal 渐变 sprite，DOFillAmount 0.25s 平滑；升级脉冲=打满+DOPunchScale，仅 newLevel>mLastLevel 时播防 ResetRunData 误触发）；Txt_Exp 复用为条上小字；左列等级/生命/攻击、中上时间(轮询 IWaveSystem.ElapsedTime 整秒才刷 string)、右上击杀/金币。数据层配套：GameModel 新增 `KillCount`（KillEnemyCommand 里 ++，ResetRunData 清零）和 `ExpToNextLevel` 属性（消除公式双写）。
**预制体/场景（全程 Unity MCP execute_code）**：生成 `Assets/Res/Art/Graph/UI/ExpGradient.png`（红→橙→黄→绿渐变 sprite）；两个 prefab 用 LoadPrefabContents+SerializedObject 接线改完；GameStart.unity / MainGame.unity 面板根实例 override 清为全屏 stretch。编译零错误（read_console + 反射双验）。
**顺手修掉早上发现的 2 个红色问题**：① GameOver 协程宿主改挂常驻 GameRoot（原来挂自己马上 SetActive(false)，首次死亡面板 AB 未缓存时协程被杀 → 结算面板打不开 + timeScale=0 卡死）；② 新建 `DamagePlayerCommand`，OnTriggerStay2D 扣血不再直写 Model。
**待用户做**：编辑器 Play 验证手感（Boot 场景启动）；标题文案可改；出包前 **Res Kit 重打 AB**（新增了 PNG，不打 AB 真机上没渐变条）。GameStartPanel.OnOpen 里的 WebGL 邀请追踪是死代码（场景直挂不触发），本次没动，需要的话另开任务。

## 08:00-09:00 字号规范 + 手机适配落地
**前置**：DOTween Setup 已完成（Assets/Resources/DOTweenSettings.asset 生成，UI 模块启用，Defines=DOTWEEN）。
**锁定横屏**：`ProjectSettings.asset` defaultScreenOrientation 4→3 (LandscapeLeft)，关闭 portrait autorotate；`game.json` 保持 landscape。
**字号规范（7 档→5 档）**：PlayerInfoPanel 的 Txt_Exp 16→24、Txt_HP 20→30、Txt_Kill/Money/Attack 20→24、Txt_Time 28→36、Txt_Lv 32→30；GameStartPanel 的 Btn_Start 48→40，Txt_Title 96 不变，说明文字 24 不变。同时把 TMP 的 m_fontSizeMin/Max 从 18/72 改为字号×1.5（实际 8~36/54）。
**新增适配脚本**：`Assets/Res/Scripts/UI/UICanvasAdapter.cs` 含 `UICanvasAdapter`（CanvasScaler 宽高比自适应：屏幕比 16:9 更窄时切到匹配宽度防裁切）和 `UISafeAreaFitter`（刘海避让，挂面板根节点）。`UICanvasAdapter` 挂到 GameStart/MainGame 的 Canvas；`UISafeAreaFitter` 挂到 PlayerInfoPanel 预制体。
**踩坑**：第一次给 PlayerInfoPanel 预制体加 fitter 时脚本还没编译完，导致保存时产生 missing script（m_Script: {fileID: 0}）。修复：用 GameObjectUtility.RemoveMonoBehavioursWithMissingScript 清理，等脚本编译完成后再加；对仍无法解析的 fileID 0 直接改 YAML 写正确 MonoScript 引用（guid=3d5905531f0827a4496b1650dbae7895）。
**编译与 AB**：零编译错误；ResKitEditorAPI.BuildAssetBundles() 重新构建 WebGL AB，AssetBundles/WebGL/ 下 gamestart_unity、maingame_unity、font、uiprefab 等 manifest 与 bundle 已更新。
