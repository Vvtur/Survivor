# 2026-08-31

## 过场加载动画：代码简化重构（已完成）

**背景**：用户反馈 `SceneTransition.cs` + `LoadingPanel.cs` 逻辑太绕、理不清，要求保留视觉（圆形遮罩展开 + 标题 + 进度条 + 百分比 + 背景）简化代码。用户明确选择"只简化代码，不动附带问题"。

**改动**：
- 重写 `Assets/Res/Scripts/UI/LoadingPanel.cs`（428 行两文件 → 单文件约 250 行）
- 删除 `Assets/Res/Scripts/UI/SceneTransition.cs`（含 .meta）
- 调用点 5 行：`GameStartPanel.cs:28/32`、`GameOverPanel.cs:19/24`、`GameRoot.cs:38`
  → `LoadingPanel.IsTransitioning` / `LoadingPanel.SwitchScene(sceneName, loaderFactory, onComplete)`

**三个核心简化点**：
1. 协程宿主改常驻 `UIKit.Root`（UIRoot.Instance 在 `UIRoot.cs:43` DontDestroyOnLoad，且三个场景都无预置实例 → 必然走 Instantiate 分支），消灭双层协程与宿主中途切换，同时去掉 `host` 参数
2. 开面板改用 `UIManager.Instance.OpenUIAsync(keys, callback)` 直接拿实例（UIKit.OpenPanelAsync 内部同一条链路，但它把回调里的 panel 扔了、只用 `while(!loaded) yield WaitForEndOfFrame`），删掉每帧 `FindFirstObjectByType` 扫描
3. 两阶段进度合并成一个 while；`FallbackLoad` 并入 `LoadSceneRoutine(panel=null)`；错峰改用 DOTween `SetDelay`

**顺带修掉**（简化副产品）：
- 旧代码超时兜底路径的宿主是 Boot 场景的 GameRoot，切场景时被销毁 → 协程被杀 → `IsTransitioning` 永久卡 true → "开始游戏"按钮彻底失效。换 UIRoot 后 finally 必定执行
- 第二次过场进度条满格起跳（OnShow 里补了 mDisplayed/mTarget/mLastPct 复位）
- Rt_Content 原始位置每次 Show 重读会漂移（改为只记一次）

**验证**：refresh_unity force+compile → read_console 0 error/warning；execute_code 反射确认 SwitchScene/IsTransitioning 存在于 Assembly-CSharp、旧 SceneTransition 类型已消失。

**未处理（用户决定不动）**：
- LoadingPanel 未打进任何 AB（所有 manifest 都搜不到）→ 真机每次过场先白等 10s 超时再无动画裸切。目录 meta `Assets/Res/Art/UIPrefab.meta` 已标记 uiprefab，重打 AB 即可包含
- GameRoot 缺 DontDestroyOnLoad（类注释说常驻但没写）→ 影响 PlayerController.cs:274 首次死亡时找不到常驻宿主
